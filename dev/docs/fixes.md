The Coding AI SHOULD NOT TOUCH THIS FILE

What needs to be fixed now

This temporary file, will be removed from git once development is done.

You do not change anything until discussed and approved. Only consult. Remember the instructions.md. 
-------------------------------------------------
======
1111
2222
======
beep 
checkin and push with markdown msg: [issue number #1](link to the github issue) 
beep
close the issue with a comment. 
The comment should have each item with a list of filenames (no path, just the filename with the extension)
beep
then open a new issue #3 
# e2e tests:
1. settings page on app start.  
    All texts and objects shown
    compare texts from config with config text
    disabled correctly
    enabled correctly
2. logged off to logged on
     pop ups work
beep
complete what can and should be checked that isn't in the 
run test


Make an e2e webtest test set that checks with the current fontsize and layout that what I now see (which is good) is what I get. 

It presses the menu 
use only a free framework. (Do we have one?)  No need to do visual tests. Just according to the generated html. 

