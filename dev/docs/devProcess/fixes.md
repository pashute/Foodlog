Filename:  temporary file. Not important what its called. 
Version:  temporary file. Not important version

The Coding AI SHOULD NOT TOUCH THIS FILE
This list is for the developer, NOT for the AI Coder. 
AI Coder gets its instructions in the chat and works with the todo.md file

---------------------------
i'm afraid to let you touch requirements.  
can you promise to fix only the lines i let you fix?
and stop after fix for approval?

------------------------------

## **web** batch
### `expo-auth-session` + GIS token flow
- [ ] placeholder for steps

### redirect: foodlog domain 
/# Notes to be removed after ai determines:  
/#  1. in final production what will that be? did we decide how to publish? at first we wanted cheasy swiss or something like that... then we changed to github? then decided no web at all. Can a callback be made to github. 

/# 2. e.g. `https://foodlog.com/auth/callback` (or `localhost:8081` dev)

/# 3. discuss manual testing and human-in-the-loop hybrid manual-auto testing. 

- [ ] placeholder for steps

- callme.md

- [ ] after agreed and implemented, summarize implementation in issues.md, stop and wait for user's input. 

## Batch **desktop (Tauri)**

- `expo-auth-session` + PKCE + Tauri deep-link plugin
- redirect: i.e. `foodlog://oauth/callback`
- packaged via `tauri build` locally

##  **android**
- `expo-auth-session` + PKCE
- redirect: `com.yourapp:/oauth2redirect`
- packaged via EAS Build → APK

## **ios** ⚠️ requires Apple Developer account ($99/yr)
- ` therefor deferred. write remarks only in files and return not implemented yet
- `expo-auth-session` + PKCE
- redirect: `com.yourapp:/oauth2redirect`
- packaged via EAS Build → IPA → TestFlight

- callme.md

- [ ] discuss and wait for user's input

## Batch: production config and sheets fix tests and test 

- [ ] Fix all focused config and sheet test to run correctly in production dev stage. 

- [ ] Test: Run focused config and sheet tests
- beep.ps1
- [ ] Test: Run full test suite
- beep.ps1
- [ ] Test: Commit and push approved changes
- beep.ps1
- [ ] Manual: Guide user through manual running and validation
- callme.ps1
- [ ] Ask for next batch



-------------------------------

## Final  

- [ ]  Remove unit test for version number. This is not a part of the functionality but rather a programming issue. 

## Sheet creation & header — audit (requirements.md / develop.md / features)

