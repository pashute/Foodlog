// Filename: fileMock.js  Version 0.1.0
// Jest stub for image imports (see package.json jest.moduleNameMapper) —
// Jest can't parse binary PNGs, so image imports resolve to this string instead.

module.exports = 'test-file-stub'
