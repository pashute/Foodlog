// Foodlog brand identity: single source of truth for name, wordmark font, and palette.
// Exact spacing/shades/positioning is deferred to a future "Look and Feel" feature.
export const appConfig = {
  name: 'Foodlog',
  logoFont: "'Quicksand', system-ui, sans-serif", // rounded, friendly, reads well small
  colors: {
    primary: '#2e7d32',   // green — used for the logo and primary actions
    background: '#fafafa',
    text: '#1c1c1c',
  },
}
